# Railway service config for the TypeScript agent sidecar.
# In the Railway dashboard, set this service's Root Directory to "/agent".

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bun src/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
